#include <stdio.h>

int main() {
    char name[100];
    
    // Input a string (name)
    scanf("%s", name);
    
    // Output the greeting with the name
    printf("Hello, %s\n", name);
    
    return 0;
}
